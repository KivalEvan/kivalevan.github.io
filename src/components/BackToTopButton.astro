<div class="btt">
    <a class="btt-button" href="#top-of-page">
        <svg width="100" height="100" viewBox="0 0 100 100" version="1.1" xlmns="http://www.w3.org/200/svg"
            title="Back to top">
            <circle cx="50" cy="50" r="50" fill="#333333" />
            <circle cx="50" cy="50" r="40" stroke="#ffffff" stroke-width="5" fill="transparent" />
            <polygon points="50 25, 75 70, 50 50, 25 70" fill="#ffffff" />
        </svg>
    </a>
</div>

<style lang="scss">
    @use '../styles/_var' as *;
    @use 'sass:math';

    $radius: $wrapper-padding * 1.25;

    .btt {
        display: block;
        position: sticky;
        bottom: 1em;
        right: 1em;
        margin-right: auto;
        margin-left: auto;
        margin-top: auto;
        margin-bottom: 1em;
        width: 100%;
        max-width: calc(#{$wrapper-readable-size} + #{$radius * 4});

        &-button {
            display: none;
            position: relative;
            width: $radius;
            height: $radius;
            margin-left: auto;
            margin-right: 1em;

            svg {
                width: $radius;
                height: $radius;
                transition: transform 0.25s;

                &:hover {
                    transform: translate(0, -0.25em);
                }
            }

            @media (min-height: 100vh) {
                display: block;
            }
        }
    }
</style>